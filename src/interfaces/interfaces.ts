export interface IInfoItem {
  id: number;
  icon: string;
  title: string;
  subtitle: string;
  content: string;
}

export interface IUser {
  name: {
    title: string;
    first: string;
    last: string
  }
}

